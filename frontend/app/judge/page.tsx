import { JudgeMode } from "../../components/judge-mode";

export const metadata = {
  title: "Judge Mode — PassportGate",
  description: "Ask LaunchProof PassportGate before an AI agent hires and pays an ASP.",
};

export default function JudgePage() {
  return <JudgeMode />;
}
