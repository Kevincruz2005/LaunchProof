import { RehearsalForm } from "../../components/rehearsal-form";

export const metadata = { title: "Rehearse a service" };
export default function RehearsePage() { return <main className="page"><section className="page-title"><p className="eyebrow">Bounded paid execution</p><h1>Rehearse a Launch Contract</h1><p>One fixed sample, one safe invalid call, exactly three fresh challenges, and no automatic target retry.</p></section><RehearsalForm expanded /></main>; }
